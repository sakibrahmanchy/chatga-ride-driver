<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
  >
    <fragment xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment" />
    <!-- TODO: Update blank fragment layout -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:id="@+id/service_not_available"
        android:background="@color/colorButtonBlack"
        android:textColor="@android:color/white"
        android:padding="10dp"
        android:textSize="18sp"
        android:text="For ride sharing,Please come to our office or call us +8801675908553"/>
    <ImageView
        android:layout_width="wrap_content"
        android:src="@drawable/ic_traffic_mode"
        android:paddingTop="20dp"
        android:layout_gravity="left"
        android:id="@+id/traffice_mode"
        android:layout_height="50dp" />


</FrameLayout>
